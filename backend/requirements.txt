# Sentra backend — runtime dependencies (exact pins).
# Python 3.14 · Django 5.2 LTS line.
#
# Version note (see SENTRA_BUILD_SPEC.md §2): the design doc names "Django 6.0 /
# Python 3.14" as targets. Django 6.x exists now but is NOT an LTS; per the build
# prompt we stay on the Django 5.2 LTS line. DRF 3.18 is the current stable
# release (spec mentions 3.16 — newer is fine, pinned exactly here).

Django==5.2.17
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-filter==26.1
drf-spectacular==0.30.0
django-cors-headers==4.9.0
psycopg[binary]==3.3.5
python-dotenv==1.2.3
gunicorn==26.2.0
whitenoise==6.12.0
argon2-cffi==25.1.0
openpyxl==3.1.5
dj-database-url==3.1.2
